using UnityEngine;

namespace Puzzle {
  public class QuestInteractable : MonoBehaviour {
    [SerializeField]
    private void Start() { }

    // Update is called once per frame
    private void Update() { }
  }
}
